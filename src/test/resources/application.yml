kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  worker:
    controllers:
      type: STATIC
      static:
        endpoints:
          - host: localhost
  url: https://mysuperhost.com/kestra/ui

micronaut:
  server:
    port: 59443
